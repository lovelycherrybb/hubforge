# HubForge 测试用例文档

> **版本**: V1.0
> **编写日期**: 2026-07-24
> **依据文档**: PRD V1.0（2026-07-23 评审修订版）
> **用例总数**: 85 条

---

## 一、M1: 用户与认证（18 条）

| 用例编号 | 所属模块 | 用例标题 | 前置条件 | 测试步骤 | 预期结果 | 优先级 | 类型 |
|----------|----------|----------|----------|----------|----------|--------|------|
| TC-001 | M1 | 正常注册新用户 | 1. 系统可访问 2. 邮箱 `new@example.com` 未注册 | 1. 访问注册页 2. 输入邮箱 `new@example.com`、密码 `Abcd@1234` 3. 点击"注册" | 1. 注册成功，提示"注册成功" 2. 跳转登录页或自动登录 3. 数据库中存在该用户记录，密码已 bcrypt 加密 | P0 | 功能 |
| TC-002 | M1 | 重复邮箱注册 | 1. 邮箱 `exist@example.com` 已注册 | 1. 访问注册页 2. 输入邮箱 `exist@example.com`、密码 `Abcd@1234` 3. 点击"注册" | 1. 注册失败，提示"该邮箱已被注册" 2. 数据库中不新增记录 | P0 | 边界 |
| TC-003 | M1 | 弱密码注册 | 1. 系统可访问 | 1. 访问注册页 2. 输入邮箱 `test@example.com`、密码 `123` 3. 点击"注册" | 1. 注册失败，提示密码不符合要求（至少 8 位，含大小写字母和数字） | P0 | 边界 |
| TC-004 | M1 | 邮箱格式错误注册 | 1. 系统可访问 | 1. 访问注册页 2. 输入邮箱 `invalid-email`、密码 `Abcd@1234` 3. 点击"注册" | 1. 前端校验提示"请输入有效的邮箱地址" 2. 不发送注册请求 | P1 | 边界 |
| TC-005 | M1 | 正常登录 | 1. 用户 `user@example.com` 已注册且已激活 | 1. 访问登录页 2. 输入邮箱和正确密码 3. 点击"登录" | 1. 登录成功，跳转门户首页 2. 浏览器写入 httpOnly Cookie（含 JWT） 3. Cookie 属性：HttpOnly=true, SameSite=Strict/Lax, Path=/ | P0 | 功能 |
| TC-006 | M1 | 错误密码登录 | 1. 用户 `user@example.com` 已注册 | 1. 访问登录页 2. 输入正确邮箱、错误密码 3. 点击"登录" | 1. 登录失败，提示"邮箱或密码错误" 2. 不写入 Cookie | P0 | 功能 |
| TC-007 | M1 | 连续错误密码锁定账号 | 1. 用户 `user@example.com` 已注册，未锁定 | 1. 连续 5 次输入错误密码并点击登录 | 1. 第 5 次失败后提示"账号已锁定，请 15 分钟后重试" 2. 第 6 次即使输入正确密码也提示锁定 | P0 | 安全 |
| TC-008 | M1 | 锁定 15 分钟后自动解锁 | 1. 用户已被锁定（接 TC-007） | 1. 等待 15 分钟 2. 输入正确密码登录 | 1. 登录成功 2. 账号恢复正常状态 | P0 | 功能 |
| TC-009 | M1 | 未激活用户登录 | 1. 用户已创建但未激活（租户管理员邀请但未点激活链接） | 1. 访问登录页 2. 输入邮箱和临时密码 3. 点击"登录" | 1. 登录失败，提示"账号未激活，请先通过激活邮件激活账号" | P0 | 边界 |
| TC-010 | M1 | 忘记密码-正常流程 | 1. 用户 `user@example.com` 已注册且已激活 | 1. 点击"忘记密码" 2. 输入注册邮箱 3. 点击"发送验证码" 4. 查收邮件获取 6 位验证码 5. 输入验证码 6. 设置新密码 `NewPass@123` 7. 点击"重置密码" | 1. 提示"验证码已发送" 2. 收到验证码邮件 3. 重置成功，提示"密码已重置" 4. 用旧密码登录失败，新密码登录成功 | P0 | 功能 |
| TC-011 | M1 | 忘记密码-验证码过期 | 1. 已发送验证码 2. 等待超过 15 分钟 | 1. 输入已过期的验证码 2. 点击"验证" | 1. 提示"验证码已过期，请重新获取" | P0 | 边界 |
| TC-012 | M1 | 忘记密码-邮箱不存在 | 1. 系统可访问 | 1. 点击"忘记密码" 2. 输入未注册邮箱 `ghost@example.com` 3. 点击"发送验证码" | 1. 为安全起见，提示"如果该邮箱已注册，验证码已发送"（不泄露邮箱是否已注册） 2. 不发送实际邮件 | P1 | 安全 |
| TC-013 | M1 | 正常登出 | 1. 用户已登录 | 1. 点击用户头像 2. 点击"登出" | 1. 跳转登录页 2. 浏览器 httpOnly Cookie 已清除 3. 访问需认证页面被重定向到登录页 | P0 | 功能 |
| TC-014 | M1 | httpOnly Cookie iframe 自动共享 | 1. 用户已登录 HubForge（主域） 2. 已注册一个同域部署的应用 | 1. 在门户首页点击该应用 2. 打开浏览器开发者工具查看 iframe 请求头 | 1. iframe 请求自动携带 httpOnly Cookie 2. 应用内请求认证成功，无需额外传 token 3. JavaScript 无法通过 `document.cookie` 读取 JWT 内容 | P0 | 功能 |
| TC-015 | M1 | Cookie 滑动过期 24h | 1. 用户已登录 2. Cookie 有效期 24h | 1. 登录后 23 小时内持续有活动 2. 23 小时后再次操作 | 1. Cookie 自动续期，用户无需重新登录 2. 检查新 Cookie 的过期时间已更新 | P1 | 功能 |
| TC-016 | M1 | Cookie 过期自动跳转登录 | 1. 用户已登录 2. Cookie 已过期（24h 无活动） | 1. 等待 Cookie 过期 2. 访问门户首页 | 1. 自动跳转登录页 2. 提示"登录已过期，请重新登录" | P0 | 功能 |
| TC-017 | M1 | 空字段注册 | 1. 系统可访问 | 1. 访问注册页 2. 不输入任何字段 3. 点击"注册" | 1. 前端校验提示"请输入邮箱"和"请输入密码" 2. 不发送请求 | P1 | 边界 |
| TC-018 | M1 | 注册后密码存储安全性 | 1. 新用户注册成功 | 1. 查看数据库 users 表对应记录 | 1. 密码字段为 bcrypt 哈希值（以 `$2b$` 开头） 2. 明文密码未存储在任何地方 | P0 | 安全 |

---

## 二、M2: 多租户（14 条）

| 用例编号 | 所属模块 | 用例标题 | 前置条件 | 测试步骤 | 预期结果 | 优先级 | 类型 |
|----------|----------|----------|----------|----------|----------|--------|------|
| TC-019 | M2 | 主租户创建新租户 | 1. 以主租户管理员登录 | 1. 进入"管理"→"租户管理" 2. 点击"创建租户" 3. 填写租户名称"测试租户A"、配额（默认）、管理员邮箱 `admin-a@example.com` 4. 点击"创建" | 1. 租户创建成功 2. 租户列表中出现"测试租户A" 3. 管理员邮箱收到激活邮件 4. 数据库 tenants 表新增记录 | P0 | 功能 |
| TC-020 | M2 | 编辑租户信息 | 1. 租户"测试租户A"已存在 | 1. 进入租户管理 2. 点击"测试租户A"的编辑按钮 3. 修改名称为"测试租户A-改" 4. 保存 | 1. 修改成功 2. 租户列表中名称已更新 | P1 | 功能 |
| TC-021 | M2 | 停用租户 | 1. 租户"测试租户A"状态为"启用" 2. 以主租户管理员登录 | 1. 进入租户管理 2. 点击"测试租户A"的"停用"按钮 3. 确认操作 | 1. 租户状态变为"停用" 2. 该租户下所有用户无法登录 3. 该租户的应用在门户中不可见 | P1 | 功能 |
| TC-022 | M2 | 启用已停用租户 | 1. 租户"测试租户A"状态为"停用" | 1. 进入租户管理 2. 点击"测试租户A"的"启用"按钮 | 1. 租户状态恢复为"启用" 2. 该租户用户可正常登录 | P1 | 功能 |
| TC-023 | M2 | 数据隔离-租户A看不到租户B数据 | 1. 租户A有用户 userA 和应用 appA 2. 租户B有用户 userB 和应用 appB 3. userA 已登录 | 1. userA 查看门户首页应用列表 2. userA 调用 API 查看用户列表 3. userA 尝试通过 API 访问 appB 的数据 | 1. 应用列表只显示 appA，不显示 appB 2. 用户列表只包含租户A的用户 3. API 返回 403 或空数据 | P0 | 安全 |
| TC-024 | M2 | 数据隔离-RLS 验证 | 1. 两个租户各有数据 2. 有数据库直接访问权限 | 1. 用租户A的数据库连接（设置 tenant_id）查询数据 2. 用租户B的数据库连接查询数据 | 1. 租户A只能看到 tenant_id=A 的行 2. 租户B只能看到 tenant_id=B 的行 3. RLS 策略在数据库层面生效 | P0 | 安全 |
| TC-025 | M2 | 主租户查看所有租户统计 | 1. 以主租户管理员登录 2. 系统中存在多个租户 | 1. 进入"管理"→"租户总览" | 1. 显示所有租户列表 2. 每个租户显示：名称、状态、用户数、应用数 3. 主租户不显示在列表中（或标注为主租户） | P0 | 功能 |
| TC-026 | M2 | 主租户配置租户配额 | 1. 以主租户管理员登录 | 1. 进入"测试租户A"的配置页 2. 修改最大用户数为 50、最大应用数为 10 3. 保存 | 1. 配置保存成功 2. 租户A后续创建用户/应用受新配额限制 | P1 | 功能 |
| TC-027 | M2 | 租户配额-用户数达到上限 | 1. 租户A的最大用户数配额为 5 2. 租户A已有 5 个用户 | 1. 以租户A管理员登录 2. 尝试创建第 6 个用户 | 1. 创建失败 2. 提示"已达到用户数配额上限（5），请联系管理员扩容" | P0 | 边界 |
| TC-028 | M2 | 租户配额-应用数达到上限 | 1. 租户A的最大应用数配额为 3 2. 租户A已注册 3 个应用 | 1. 以租户A管理员登录 2. 尝试注册第 4 个应用 | 1. 注册失败 2. 提示"已达到应用数配额上限（3），请联系管理员扩容" | P0 | 边界 |
| TC-029 | M2 | 租户管理员邀请激活流程 | 1. 主租户管理员已创建租户并填写管理员邮箱 | 1. 查看管理员邮箱收到的激活邮件 2. 点击激活链接 3. 设置新密码 4. 用新密码登录 | 1. 激活链接有效且指向正确页面 2. 设置密码成功 3. 首次登录成功 4. 进入租户管理后台 5. 账号状态变为"已激活" | P0 | 功能 |
| TC-030 | M2 | 租户管理员邀请-重复激活 | 1. 激活链接已被使用过 | 1. 再次点击同一激活链接 | 1. 提示"该链接已失效"或"账号已激活" 2. 不允许重复修改密码 | P1 | 边界 |
| TC-031 | M2 | 非主租户用户无法访问全局管理 | 1. 以租户A管理员登录（非主租户） | 1. 尝试通过 URL 直接访问全局管理后台 2. 检查顶部栏是否有"管理"入口 | 1. URL 访问被拦截，返回 403 或重定向 2. 顶部栏不显示全局"管理"入口 | P0 | 安全 |
| TC-032 | M2 | 租户管理员变更 | 1. 以主租户管理员登录 2. 租户A当前管理员为 adminA | 1. 进入"测试租户A"配置 2. 点击"变更管理员" 3. 选择或输入新管理员邮箱 4. 确认 | 1. 变更成功 2. 新管理员收到激活/通知邮件 3. 旧管理员权限降级为普通用户 | P1 | 功能 |

---

## 三、M3: 组织架构（10 条）

| 用例编号 | 所属模块 | 用例标题 | 前置条件 | 测试步骤 | 预期结果 | 优先级 | 类型 |
|----------|----------|----------|----------|----------|----------|--------|------|
| TC-033 | M3 | 创建根部门 | 1. 以租户管理员登录 2. 组织架构为空 | 1. 进入"组织架构"管理 2. 点击"创建部门" 3. 输入名称"总公司" 4. 保存 | 1. 部门创建成功 2. 左侧树形显示"总公司"节点 3. 数据库中记录 tenant_id 和正确的 parent_id（null） | P0 | 功能 |
| TC-034 | M3 | 创建子部门 | 1. "总公司"部门已存在 | 1. 选中"总公司"节点 2. 点击"添加子部门" 3. 输入名称"生产部" 4. 保存 | 1. "生产部"作为"总公司"的子节点显示 2. 层级关系正确 | P0 | 功能 |
| TC-035 | M3 | 编辑部门名称 | 1. "生产部"已存在 | 1. 选中"生产部" 2. 点击"编辑" 3. 修改名称为"制造部" 4. 保存 | 1. 名称更新为"制造部" 2. 树形结构即时刷新 | P1 | 功能 |
| TC-036 | M3 | 删除部门 | 1. "质检部"已存在，且该部门下无用户 | 1. 选中"质检部" 2. 点击"删除" 3. 确认删除 | 1. 部门删除成功 2. 树形中不再显示该节点 | P1 | 功能 |
| TC-037 | M3 | 删除含用户的部门 | 1. "生产部"下有 3 个用户 | 1. 选中"生产部" 2. 点击"删除" | 1. 提示"该部门下有 3 个用户，请先迁移用户" 2. 不允许直接删除 | P1 | 边界 |
| TC-038 | M3 | 移动部门节点 | 1. "IT部"为"总公司"的子部门 2. "分公司"已存在 | 1. 拖拽"IT部"到"分公司"下 2. 确认移动 | 1. "IT部"变为"分公司"的子节点 2. 层级关系更新正确 | P1 | 功能 |
| TC-039 | M3 | 部门层级超过 8 层软提示 | 1. 已创建 7 层嵌套部门 | 1. 在第 7 层部门下创建子部门 | 1. 创建成功但显示提示"建议组织层级不超过 8 层" 2. 不硬性阻止创建 | P1 | 边界 |
| TC-040 | M3 | 用户分配到部门 | 1. 用户 `user@example.com` 已注册 2. "生产部"已存在 | 1. 进入"生产部"详情 2. 点击"添加成员" 3. 选择 `user@example.com` 4. 确认 | 1. 用户出现在"生产部"成员列表中 2. 用户所属部门显示为"生产部" | P0 | 功能 |
| TC-041 | M3 | 组织级权限继承-取并集 | 1. "生产部"被授予 app.inspection.view 权限 2. 用户A属于"生产部" 3. 用户A个人被授予 app.inspection.submit 权限 | 1. 查询用户A的有效权限 | 1. 用户A同时拥有 inspection.view 和 inspection.submit 2. 权限为个人权限 ∪ 组织权限的并集 | P1 | 功能 |
| TC-042 | M3 | H5 端组织架构只读 | 1. 以租户管理员在手机浏览器登录 | 1. 在 H5 端查看组织架构 2. 尝试编辑/删除/创建部门 | 1. 组织架构以可折叠列表形式正常显示 2. 不显示任何编辑/删除/创建按钮 3. 仅可查看 | P0 | 功能 |

---

## 四、M4: 权限体系（10 条）

| 用例编号 | 所属模块 | 用例标题 | 前置条件 | 测试步骤 | 预期结果 | 优先级 | 类型 |
|----------|----------|----------|----------|----------|----------|--------|------|
| TC-043 | M4 | 主租户分配框架权限 | 1. 以主租户管理员登录 2. 应用"巡检系统"已注册（appKey=inspection） | 1. 进入"管理"→"框架权限" 2. 选择用户 userA 3. 勾选 `app.inspection.access` 4. 保存 | 1. 权限分配成功 2. userA 登录后在门户中可看到"巡检系统"应用 | P0 | 功能 |
| TC-044 | M4 | 租户管理员分配应用权限 | 1. 以租户管理员登录 2. 应用"巡检系统"已注册 3. 应用声明权限 `inspection.submit` | 1. 进入"管理"→"应用权限" 2. 选择用户 userB 3. 勾选 `inspection.submit` 4. 保存 | 1. 权限分配成功 2. userB 在应用内拥有提交巡检的权限 | P0 | 功能 |
| TC-045 | M4 | 越权拦截-租户管理员无法分配框架权限 | 1. 以租户管理员登录 | 1. 通过 API 请求分配框架权限 `app.inspection.access` 给某用户 | 1. API 返回 403 Forbidden 2. 提示"无权分配框架权限" 3. 权限未变更 | P0 | 安全 |
| TC-046 | M4 | 权限冲突-个人+组织取并集 | 1. 用户A属于"生产部" 2. "生产部"被授予 `app.inspection.view` 3. 用户A个人被授予 `app.inspection.approve` | 1. 查询用户A的有效权限列表 | 1. 用户A拥有 view 和 approve 两个权限 2. 不丢失任何一方的权限 3. 取并集生效 | P0 | 功能 |
| TC-047 | M4 | 无拒绝规则验证 | 1. 用户A不属于任何被授予权限的部门 2. 用户A个人未被授予 `app.inspection.view` | 1. 用户A尝试访问"巡检系统" 2. 查询用户A的权限列表 | 1. 应用不在用户A的门户中可见（无 access 权限） 2. 权限列表中不包含该权限 3. 系统无"拒绝"状态，只有"授予"和"未授予" | P0 | 功能 |
| TC-048 | M4 | 应用通过 config 声明权限 | 1. 应用的 hubforge.config.json 声明了 3 个权限 | 1. 在 HubForge 后台注册该应用 2. 查看权限配置页面 | 1. 页面显示应用声明的 3 个权限 key 和 label 2. 管理员可选择将每个权限标记为"框架权限"或"应用权限" | P0 | 功能 |
| TC-049 | M4 | 框架权限校验在框架层执行 | 1. 用户A无 `app.inspection.access` 权限 2. 手动构造 iframe URL 直接访问应用 | 1. 直接在浏览器地址栏输入应用 URL（绕过门户） | 1. 框架中间件拦截请求 2. 返回 403 或重定向到"无权限"页面 3. 应用内容不可见 | P0 | 安全 |
| TC-050 | M4 | 移除用户权限后立即生效 | 1. 用户A有 `app.inspection.access` 权限 | 1. 管理员移除用户A的 `app.inspection.access` 权限 2. 用户A刷新门户页面 | 1. "巡检系统"不再出现在用户A的应用列表中 2. 权限变更即时生效 | P0 | 功能 |
| TC-051 | M4 | 组织权限继承-只做加法 | 1. 用户A个人被授予 `app.inspection.submit` 2. 用户A所属"生产部"被授予 `app.inspection.view` | 1. 管理员修改"生产部"的权限（去掉 view，加上 approve） 2. 查询用户A的有效权限 | 1. 用户A仍保留 `submit`（个人权限不受影响） 2. 用户A新增 `approve`（继承新组织权限） 3. 用户A失去 `view`（组织不再授予） | P1 | 功能 |
| TC-052 | M4 | 非管理员用户无法访问权限管理 | 1. 以普通终端用户登录 | 1. 尝试通过 URL 访问权限管理页面 2. 检查导航菜单 | 1. URL 访问返回 403 2. 导航菜单中无"管理"入口 | P0 | 安全 |

---

## 五、M5: 应用管理与门户（15 条）

| 用例编号 | 所属模块 | 用例标题 | 前置条件 | 测试步骤 | 预期结果 | 优先级 | 类型 |
|----------|----------|----------|----------|----------|----------|--------|------|
| TC-053 | M5 | 注册 PC 类型应用 | 1. 以租户管理员登录 2. 应用已部署且可访问 | 1. 进入应用管理 2. 点击"注册应用" 3. 填写名称"数据看板"、URL `https://dashboard.example.com`、类型选择"PC" 4. 保存 | 1. 注册成功 2. 应用列表中出现"数据看板" 3. 类型标记为"PC" | P0 | 功能 |
| TC-054 | M5 | 注册 H5 类型应用 | 1. 以租户管理员登录 | 1. 注册应用 2. 填写名称"巡检工具"、URL、类型选择"H5" 3. 保存 | 1. 注册成功 2. 类型标记为"H5" 3. H5 端门户可见，PC 端门户不可见 | P0 | 功能 |
| TC-055 | M5 | 注册双端应用 | 1. 以租户管理员登录 | 1. 注册应用 2. 类型选择"PC+H5" 3. 保存 | 1. 注册成功 2. PC 端和 H5 端门户均可见该应用 | P0 | 功能 |
| TC-056 | M5 | iframe 正常加载应用 | 1. 应用"巡检系统"已注册 2. 用户有访问权限 | 1. 在 PC 端门户点击"巡检系统" 2. 观察 iframe 加载情况 | 1. iframe 正常加载应用页面 2. 应用内容充满 iframe 区域 3. 框架顶部栏仍可见（~48px） | P0 | 功能 |
| TC-057 | M5 | PC 端门户-顶部栏 | 1. 以普通用户登录 PC 端 | 1. 观察顶部栏内容 | 1. 显示 HubForge Logo 2. 显示"首页"按钮 3. 显示当前视图/应用名 4. 显示搜索框 5. 显示用户头像 6. 顶部栏高度约 48px，固定定位 | P0 | 功能 |
| TC-058 | M5 | PC 端门户-首页应用列表 | 1. 以普通用户登录 2. 用户有权访问 3 个应用 | 1. 访问门户首页 | 1. 显示 3 个应用卡片 2. 卡片包含图标和应用名称 3. 仅显示用户有权访问的应用 | P0 | 功能 |
| TC-059 | M5 | PC 端门户-应用视图空间 | 1. 用户已进入某个应用 | 1. 观察应用视图布局 | 1. 顶部栏占约 48px 2. 应用 iframe 占据剩余全部空间 3. iframe 无多余边距 | P0 | 功能 |
| TC-060 | M5 | H5 端门户-应用卡片 | 1. 以普通用户在手机浏览器登录 | 1. 访问 H5 门户首页 | 1. 显示应用卡片网格（2列） 2. 卡片包含应用名称和图标 3. 仅显示用户有权访问的 H5 类型应用 | P0 | 功能 |
| TC-061 | M5 | H5 端门户-全屏应用视图 | 1. 用户在 H5 端 | 1. 点击某个应用卡片 | 1. 跳转到全屏应用视图 2. iframe 占满屏幕 3. 顶部显示返回按钮 4. 非混排模式（首页和应用为独立视图） | P0 | 功能 |
| TC-062 | M5 | H5 端-底部导航 | 1. 以普通用户在 H5 端登录 | 1. 观察底部导航栏 | 1. 显示"首页"、"应用"、"我的"三个导航项 2. 点击可切换对应视图 | P1 | 功能 |
| TC-063 | M5 | 管理员可见"管理"入口 | 1. 以租户管理员登录 | 1. 查看 PC 端顶部栏 | 1. 顶部栏显示齿轮图标"管理"入口 2. 点击可进入租户管理后台 | P0 | 功能 |
| TC-064 | M5 | 普通用户不可见"管理"入口 | 1. 以普通终端用户登录 | 1. 查看 PC 端顶部栏 | 1. 顶部栏无齿轮图标 2. 不显示"管理"入口 | P0 | 安全 |
| TC-065 | M5 | 搜索功能-按应用名搜索 | 1. 用户有权访问"巡检系统"、"数据看板"、"工作台" | 1. 在搜索框输入"巡检" 2. 观察搜索结果 | 1. 应用列表过滤，仅显示"巡检系统" 2. 清空搜索框后恢复完整列表 | P1 | 功能 |
| TC-066 | M5 | 搜索功能-无结果 | 1. 用户有权访问的应用均不含"报表"关键字 | 1. 在搜索框输入"报表" | 1. 显示空结果或"未找到匹配的应用"提示 | P2 | 边界 |
| TC-067 | M5 | iframe 离开时销毁 | 1. 用户正在使用某应用（iframe 已加载） | 1. 点击"首页"返回 2. 检查 DOM 中的 iframe 元素 | 1. iframe 从 DOM 中移除（销毁） 2. 不占用后台资源 | P1 | 功能 |

---

## 六、安全测试（10 条）

| 用例编号 | 所属模块 | 用例标题 | 前置条件 | 测试步骤 | 预期结果 | 优先级 | 类型 |
|----------|----------|----------|----------|----------|----------|--------|------|
| TC-068 | 安全 | SQL 注入防护-登录 | 1. 系统可访问 | 1. 在登录邮箱输入框输入 `admin' OR '1'='1' --` 2. 密码输入任意值 3. 点击登录 | 1. 登录失败 2. 不返回异常错误信息 3. 后端使用参数化查询，无 SQL 注入 | P0 | 安全 |
| TC-069 | 安全 | SQL 注入防护-搜索 | 1. 用户已登录 | 1. 在搜索框输入 `'; DROP TABLE users; --` | 1. 搜索返回空结果或正常提示 2. 数据库未受影响 3. users 表完整 | P0 | 安全 |
| TC-070 | 安全 | XSS 防护-注册 | 1. 系统可访问 | 1. 在注册邮箱输入框输入 `<script>alert('xss')</script>@test.com` 2. 提交注册 | 1. 前端/后端对输入进行转义 2. 不执行 JavaScript 3. 页面不弹出 alert 框 | P0 | 安全 |
| TC-071 | 安全 | XSS 防护-应用名显示 | 1. 租户管理员已登录 | 1. 注册应用，名称为 `<img src=x onerror=alert(1)>` 2. 普通用户查看门户首页 | 1. 应用名称被转义显示为纯文本 2. 不执行恶意脚本 3. CSP 头阻止内联脚本 | P0 | 安全 |
| TC-072 | 安全 | CSRF 防护 | 1. 用户已登录 HubForge 2. 在另一个标签页打开恶意页面 | 1. 恶意页面构造表单自动提交到 HubForge 的 API（如删除租户） | 1. 请求被拒绝 2. 返回 403 或 CSRF Token 无效 3. SameSite Cookie 属性阻止跨站请求 | P0 | 安全 |
| TC-073 | 安全 | RLS 数据隔离绕过-修改 tenant_id | 1. 以租户A用户登录 2. 有 API 调用能力 | 1. 拦截 API 请求 2. 在请求体或 header 中注入 tenant_id=租户B 3. 发送请求 | 1. 中间件从 JWT/Cookie 中提取 tenant_id，忽略请求中的 tenant_id 2. 返回数据仍为租户A的数据 3. 记录异常日志 | P0 | 安全 |
| TC-074 | 安全 | JWT 篡改 | 1. 用户已登录 2. 获取到 httpOnly Cookie 中的 JWT（通过网络抓包等） | 1. 解码 JWT payload 2. 修改 tenant_id 或 role 字段 3. 重新编码（不签名） 4. 替换 Cookie 发送请求 | 1. JWT 签名校验失败 2. 返回 401 Unauthorized 3. 用户被要求重新登录 | P0 | 安全 |
| TC-075 | 安全 | JWT 过期验证 | 1. 获取一个已过期的 JWT | 1. 使用过期 JWT 发送 API 请求 | 1. 返回 401 2. 提示"Token 已过期" 3. 自动尝试刷新 Token（滑动过期） | P0 | 安全 |
| TC-076 | 安全 | CSP 安全头 | 1. 系统可访问 | 1. 检查 HTTP 响应头 | 1. 包含 Content-Security-Policy 头 2. 限制 script-src、frame-src 等来源 3. 包含 X-Frame-Options 或 frame-ancestors 配置 | P1 | 安全 |
| TC-077 | 安全 | 敏感信息不暴露 | 1. 系统可访问 | 1. 触发一个 API 错误（如访问不存在的资源） 2. 检查错误响应内容 | 1. 不返回堆栈跟踪信息 2. 不暴露数据库结构、技术栈版本等 3. 返回友好的错误消息 | P1 | 安全 |

---

## 七、兼容性测试（8 条）

| 用例编号 | 所属模块 | 用例标题 | 前置条件 | 测试步骤 | 预期结果 | 优先级 | 类型 |
|----------|----------|----------|----------|----------|----------|--------|------|
| TC-078 | 兼容性 | PC-Chrome 90+ | 1. 安装 Chrome 90 或更高版本 | 1. 访问 HubForge 2. 完成登录、浏览门户、打开应用等核心流程 | 1. 页面正常渲染 2. 布局无错乱 3. iframe 正常加载 4. 所有交互功能正常 | P0 | 兼容性 |
| TC-079 | 兼容性 | PC-Edge 90+ | 1. 安装 Edge 90 或更高版本 | 1. 同 TC-078 步骤 | 1. 同 TC-078 预期结果 | P0 | 兼容性 |
| TC-080 | 兼容性 | PC-Safari 15+ | 1. 安装 Safari 15 或更高版本 | 1. 同 TC-078 步骤 | 1. 同 TC-078 预期结果 2. 特别注意 Cookie 和 iframe 行为与 Safari 的兼容性 | P0 | 兼容性 |
| TC-081 | 兼容性 | H5-iOS Safari 15+ | 1. iPhone，iOS 15+，Safari 浏览器 | 1. 访问 HubForge H5 端 2. 登录、浏览应用卡片、打开应用 | 1. 页面正常渲染，适配移动端 2. 应用卡片布局正确（2列） 3. 全屏 iframe 正常工作 4. 底部导航正常 | P0 | 兼容性 |
| TC-082 | 兼容性 | H5-Android Chrome 90+ | 1. Android 手机，Chrome 90+ | 1. 同 TC-081 步骤 | 1. 同 TC-081 预期结果 | P0 | 兼容性 |
| TC-083 | 兼容性 | PC 最小分辨率 1280px | 1. 浏览器窗口宽度设为 1280px | 1. 访问门户首页 2. 访问应用视图 | 1. 布局无错乱 2. 应用卡片正常显示 3. iframe 无水平滚动条 | P1 | 兼容性 |
| TC-084 | 兼容性 | H5 最小分辨率 375px | 1. 模拟 iPhone SE 宽度 375px | 1. 访问 H5 门户 2. 浏览应用卡片 3. 打开应用 | 1. 布局适配 375px 宽度 2. 应用卡片不溢出 3. 文字可读 | P1 | 兼容性 |
| TC-085 | 兼容性 | iframe 内 Cookie 共享-跨浏览器 | 1. PC + H5 各浏览器 | 1. 登录后进入应用 2. 检查 iframe 内应用的请求是否自动携带 Cookie | 1. 同域部署下所有浏览器均自动携带 httpOnly Cookie 2. 应用内认证正常 | P0 | 兼容性 |

---

## 附录

### 用例统计

| 模块 | 用例数 | P0 | P1 | P2 |
|------|--------|----|----|-----|
| M1: 用户与认证 | 18 | 11 | 5 | 0 |
| M2: 多租户 | 14 | 8 | 5 | 0 |
| M3: 组织架构 | 10 | 4 | 5 | 0 |
| M4: 权限体系 | 10 | 8 | 2 | 0 |
| M5: 应用管理与门户 | 15 | 10 | 3 | 1 |
| 安全测试 | 10 | 7 | 3 | 0 |
| 兼容性测试 | 8 | 6 | 2 | 0 |
| **合计** | **85** | **54** | **25** | **1** |

### 按类型统计

| 类型 | 用例数 |
|------|--------|
| 功能 | 42 |
| 安全 | 17 |
| 边界 | 15 |
| 兼容性 | 8 |
| **合计** | **85** |
